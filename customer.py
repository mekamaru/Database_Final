from re import A
import tkinter
from tkinter import ttk #Ttk widgets gives the application an improved look and feel
from tkinter import messagebox
import sqlite3
import os
from variables import *

def BuyBooksGUI(user_id):
    print("This is BuyBookGUI!\n Login by ", str(user_id), ".")

def UserInfoGUI(user_id):
    print("This is UserInfoGUI!\n Login by ", str(user_id), ".")